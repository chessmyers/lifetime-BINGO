<%= form_with(model: bingo_hall) do |form| %>
  <% if bingo_hall.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bingo_hall.errors.count, "error") %> prohibited this bingo_hall from being saved:</h2>

      <ul>
        <% bingo_hall.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
